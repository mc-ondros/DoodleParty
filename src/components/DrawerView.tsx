import React from 'react';

export const DrawerView: React.FC = () => {
  // Side drawer with game mode selection
  return (
    <div className="drawer-view">
      <aside className="drawer">
        {/* Game mode selection content */}
      </aside>
    </div>
  );
};
